% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_nested_list.R
\name{make_nested_list}
\alias{make_nested_list}
\title{Create a Nested List}
\usage{
make_nested_list(lgth)
}
\arguments{
\item{lgth}{Integer vector giving the length of the list at each level,
from the outermost to the innermost level.}
}
\value{
A nested list with one level for each element of \code{lgth} and with the
corresponding lengths specified by \code{lgth}.
}
\description{
This function creates a nested list with dimensions specified by \code{lgth}.
Each element at the deepest level is initialized as \code{NULL}.
}
\details{
The last element of \code{lgth} determines the length of the deepest list.
The remaining elements determine, from inside to outside, how many copies
of the current list are created at each level.

For example, \code{make_nested_list(c(2, 3))} creates a list of length 2,
where each element is a list of length 3.
}
\examples{
make_nested_list(c(2, 3))

x <- make_nested_list(c(2, 3, 4))
length(x)
length(x[[1]])
length(x[[1]][[1]])

}
