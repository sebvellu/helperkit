% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qr_safe_solve.R
\name{qr_safe_solve}
\alias{qr_safe_solve}
\title{Safe Linear Solver Using QR or Pseudoinverse Fallback}
\usage{
qr_safe_solve(mata, matb)
}
\arguments{
\item{mata}{Numeric matrix A in the linear system A x = B.}

\item{matb}{Numeric matrix or vector B in the linear system A x = B.}
}
\value{
A numeric matrix or vector giving the solution to A x = B. If
\code{qr.solve} succeeds, its result is returned. If it fails, the solution
computed using \code{mp_inv(A) \%*\% B} is returned.
}
\description{
This function solves the linear system A x = B using \code{qr.solve}.
If \code{qr.solve} fails (for example due to rank deficiency), the function
falls back to using the Moore-Penrose pseudoinverse computed by
\code{mp_inv}.
}
\details{
The function first attempts:

\code{qr.solve(mata, matb)}

If this produces an error, a pseudoinverse-based solution is used:

\code{mp_inv(mata) \%*\% matb}
}
\examples{
\dontrun{
A <- matrix(c(1, 2, 2, 4), 2)
b <- c(1, 1)
qr_safe_solve(A, b)
}

}
\seealso{
mp_inv
}
